<h2>Existing passengers</h2>

<ul>
<% @passengers.each do |passenger| %>
  <% if passenger.name != "Unknown Passenger" %>
    <li>
      <%= link_to passenger.name, passenger_path(passenger.id) %>
      <%= passenger.phone_num %>
    </li>
  <% end %>
<% end %>
</ul>